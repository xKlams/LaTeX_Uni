f = thomas(a, b, c, f)
% a = diagonale princiaple (vettore n)
% b = sottodiagonale inferiroe (vettore n-1)
% c = sottodiaognale superiore (vettore n-1)
% vettore soluzione 
b = [0;b]; % il vettore diventa quindi di n elementi e il secodo elemento si chiamerà b[2], che è quello che voglaimo
c = [c;0]; % stesso identico motivo di b, ma non ci serve davvero


